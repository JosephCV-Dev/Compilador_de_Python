"""Punto de entrada del lexer.

La implementacion real se agregara en el siguiente incremento.
Debe recorrer el codigo fuente de izquierda a derecha y devolver
tokens + errores sin depender de Flask ni del navegador.
"""


def tokenize(source):
    """Tokeniza codigo fuente Python.

    TODO:
    - Aplicar prioridad de reglas.
    - Reconocer NEWLINE, INDENT y DEDENT.
    - Reportar errores lexicos recuperables.
    """
    raise NotImplementedError("El lexer se implementara en el siguiente incremento.")
